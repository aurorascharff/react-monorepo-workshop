# Step 3 — State & Effects

Module 3 is complete in this snapshot. Derived state is computed during render,
and reusable patient search/filter behavior lives in `usePatientFilter`.
Manual `useEffect` data fetching remains for Module 4.
