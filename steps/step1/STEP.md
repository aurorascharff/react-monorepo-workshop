# Step 1 — Architecture & Reuse

Module 1 is complete in this snapshot. The Arena app has feature folders,
focused patient/journal components, a shared layout with an error boundary, and
`StatusBadge` lives in `@medix/ui` for use by both Arena and medix.com.
