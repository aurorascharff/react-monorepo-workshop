// Reference test for Module 1.
//
// The real test lives on `main` and exercises the `<StatusBadge>` you build
// in `packages/ui/src/StatusBadge.tsx`. It can't run on the starter because
// `@medix/ui` doesn't export `StatusBadge` yet — importing it here would
// crash the test runner.
//
// When you finish Module 1, copy the test body from:
// https://github.com/aurorascharff/react-monorepo-workshop/blob/main/apps/arena/src/test/StatusBadge.test.tsx

describe.skip('StatusBadge (Module 1)', () => {
  it.todo('renders the correct label for each status')
})
