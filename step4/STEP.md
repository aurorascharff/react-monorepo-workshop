# Step 4 — TanStack Query

Module 4 is complete in this snapshot. Patient and journal data are loaded with
TanStack Query, mutations invalidate the relevant journal queries, and the
patient detail route has local suspense/error handling.

The journal form is still intentionally pre-Module-5.
